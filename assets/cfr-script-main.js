window.addEventListener('load', async event => {
    /*
    const stickyHeader = document.querySelector('sticky-header');
    const stickyHeader_height = stickyHeader.getBoundingClientRect( ).height;

    if( document.documentElement.scrollTop > stickyHeader.getBoundingClientRect( ).height ) {
        stickyHeader.classList.remove('cfr-sticky_header');
    }

    window.addEventListener('scroll', event => {
        // dynamic sticky header 
        if( document.documentElement.scrollTop > stickyHeader_height ) {
            stickyHeader.classList.remove('cfr-sticky_header');
        } else {
            stickyHeader.classList.add('cfr-sticky_header');
        }

    });
    */
});

